const Supervisor = require('./src/core/Supervisor');\nconst Config = require('./src/core/Config');\n\nconst config = Config.toObject();\nconst supervisor = new Supervisor(config);\n\nif (require.main === module) {\n  supervisor.start();\n}\n\nmodule.exports = supervisor;
