'use strict';
module.exports={name:'pay',aliases:['transfer','sendmoney'],category:'economy',description:'Transfer coins to another user.',usage:'/pay <userID> <amount>',cooldown:3,role:0,async execute(ctx){const id=ctx.args[0],amount=Math.floor(Number(ctx.args[1]));if(!/^\d+$/.test(String(id||''))||!Number.isFinite(amount)||amount<=0)return ctx.reply(ctx.error(`Usage: ${ctx.prefix}pay <userID> <amount>`));if(String(id)===String(ctx.userID))return ctx.reply(ctx.error('You cannot pay yourself.'));const from=ctx.user||await ctx.db.ensureUser(ctx.userID);const to=await ctx.db.ensureUser(id);if(from.coins<amount)return ctx.reply(ctx.error('You do not have enough coins.'));from.coins-=amount;to.coins=(to.coins||0)+amount;await ctx.db.write();return ctx.reply(ctx.format('Transfer',[`Sent: ${amount} coins`,`Recipient: ${id}`,`Your balance: ${from.coins}`]));}};
