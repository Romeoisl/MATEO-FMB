'use strict';
module.exports={name:'clear',category:'utility',description:'Explain chat clearing support.',usage:'/clear',role:0,cooldown:3,async execute(ctx){return ctx.reply(ctx.format('Clear',['Messenger history is controlled by the platform; MATEO-FMB does not delete messages automatically.']));}};
