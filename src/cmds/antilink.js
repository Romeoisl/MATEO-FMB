'use strict';
module.exports={name:'antilink',aliases:['antilinks'],category:'group',description:'Enable or disable link protection.',usage:'/antilink <on|off>',role:1,async execute(ctx){const v=ctx.args[0]?.toLowerCase();if(!['on','off'].includes(v))return ctx.reply(ctx.error(`Usage: ${ctx.prefix}antilink <on|off>`));await ctx.groups.set(ctx.threadID,'antiLink',v==='on');return ctx.reply(ctx.format('Anti-link',[`Protection: ${v}`]));}};
